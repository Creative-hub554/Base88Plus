# Release v0.2.0 — first push, first green run, first automated release

Tracks the cutover from the bare web-upload skeleton to the real repository, the first CI run on GitHub, and the first release produced by the automation. Everything checked below is verified as of the issue's creation: local `main` is at `bf51f76` (13 commits, 207/207 tests, tsc + lint clean); remote `main` is still the two upload commits (`30270b3`, `d465d78`).

## 1. Push the real history

- [ ] From a local terminal (needs the interactive sign-in window):
  ```
  git push --force-with-lease=main:d465d78e09bee173056f34cfba55e7bb92a0e5fe -u origin main
  ```
- [ ] Credential Manager sign-in completes as `Creative-hub554`
- [ ] `git ls-remote origin refs/heads/main` reports `bf51f76…`

Why force: the remote holds an unrelated web-upload history; `--force-with-lease` replaces it but fails safely if anyone pushed in the meantime.

## 2. First CI run green

- [ ] Actions tab shows the first **CI** run for commit `bf51f76`
- [ ] `matrix` job green — computed legs `[22, 24]`, canary `[26]`
- [ ] `gates (22)` ✅ and `gates (24)` ✅ (blocking legs)
- [ ] `canary (26)` green, or ⚠ absorbed (non-blocking by design)
- [ ] `ci-ok` ✅ — the single check that decides everything

Triage if red: one leg failing = Node version drift; both = ordinary test failure; `matrix` = schedule classification (its log names the reason).

## 3. Automatic release v0.2.0

- [ ] A **Release** workflow run fires right after the green CI run (`workflow_run`, main-only, success-only)
- [ ] Annotated tag `v0.2.0` created, pointing at `bf51f76`
- [ ] GitHub Release published with asset `anybase-v0.2.0.zip` (source zip via `git archive`, ~300 KB)
- [ ] Re-running the workflow does nothing (tag-exists guard → strictly one tag per version)

Future releases are then just: bump `package.json` → merge → green CI → tag + zip appear.

## 4. Branch protection (ruleset)

- [ ] Settings → Rules → Rulesets → **New branch ruleset**, name `protect main`, enforcement **Active**, target `main`
- [ ] Require status checks: **`ci-ok` only** — never per-leg names (they change on Node 26's promotion day, 2026-10-28), never the canary
- [ ] Bypass list: **`github-actions[bot]`**, mode **Always** (the monthly snapshot-refresh commit needs direct push)
- [ ] Decide the PR policy (recommended: require a PR with 0 approvals — solo flow, real gate)
- [ ] Verify: a PR to `main` shows `ci-ok` as required; a direct push is rejected with `GH006`

## 5. Leak-response closure (resolved)

- [x] Audit of the public upload complete — `providers.json` contained only placeholders (`sk-or-v1-paste-here` ×2, `PASTE_KEY_HERE`); all 15 other files scanned, zero secret patterns
- [x] Key rotation stood down (or completed anyway as hygiene, if already done)
- [x] Actions cache purge — not applicable (no workflow ever ran pre-push, so no caches exist)

## 6. Automation that comes alive with the push

- [ ] **Dependabot** — weekly npm + github-actions PRs; SHA pins in `ci.yml` updated together with their `# vX.Y.Z` comments
- [ ] **Monthly CI self-check** — cron `17 7 3 * *`; first scheduled run **2026-10-03 07:17 UTC**; it also refreshes the release-schedule snapshot as a bot commit
- [ ] **Known future milestones** — Node 26 → blocking gate on 2026-10-28 (zero-touch); Node 22 EOL on 2027-04-30 → CI goes red until the engines floor is bumped to 24 (deliberate, explicit)
